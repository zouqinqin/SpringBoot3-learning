## 我在梳理财务对账业务实现交互图，根据我的要求（如下），画出页面交互图
1. 现有客户按月出账后的head表，
字段有：出账月（例如:2026-06），客户名称(mdmName)，uin（可能多个使用逗号分隔）,客户属性，总应收余额，ou,等
2. 还有head 表对应的详情表item,
字段有：出账月，uin一个一行，每行的应收余额

- 现在有一个余额对账页面，展示的是head表数据，在每一行的有一个详情按钮，点击详情，跳转详情页面，主要分为两个部分

- 上面部分是head的 关键字段，例如，总应收余额，出账月，ou名称，客户名称等
下部分是 item 多行

- 详情页面右上角有一个发送账单按钮，就会出现很多输入框，例如，客户收件人，邮件模版选择（选择一个模版，例如：余额对账单，发票等）就会出对应的邮件模版格式，查询数据库返回的
，还有附件信息，用户确认无误后，点击发送，会生成一份状态为“就蓄”的邮件，后台有一个任务会读取最新状态为“就蓄”的邮件 去发送给客户。


**- 这里业务提出了一个优化需求**，她要求说对应客户属性=财管对账客户，要求要财管负责任审批后才可以发送给用户

- 获取财管负责人的方法是通过uin去调用三方系统的 getFinanceManager 方法
- 当前的功能是在对账的后台系统，提供给财务人员使用

你能画出我当前涉及到的页面结构吗


